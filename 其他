后缀名为vsdx的流程图文件，需要安装visio professional打开

搭建python3开发环境，安装相关包：
建一个项目文件夹：
安装python3：安装到项目文件夹下（多python版本环境）
安装django：在项目文件夹中安装，和python3同一根目录，定位到此目录，执行“python3 -m pip install django”
安装数据库pymysql：定位到项目文件夹，执行“python3 -m pip install django”

前后端分离，通过配置跨域将前端页面绑定到后端
单独运行前端时，会发现无法响应数据请求，这是需要打开前端的config的index.js文件，将target配置为后端本地运行时的url，这样就能响应数据请求
target：http：//xxx.com
host:http：//xxx.com
其他默认
没有配置这一项一般会发生如下报错：
hpm error occurred while trying to proxy request /usermanage/getusername/(数据接口) from http:xxxx.com to http:yyyy.com
